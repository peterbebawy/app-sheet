# Security

- لا ترفع كلمات مرور المستخدمين أو بيانات حساسة إلى مستودع عام.
- يفضّل استخدام Firebase Authentication بدل تخزين كلمات المرور كنص داخل Realtime Database في أي نسخة إنتاجية مستقبلية.
- اضبط Firebase Realtime Database Rules بحيث لا يستطيع المستخدم غير المصرح له قراءة أو تعديل `app_data` أو `users`.
- لا تشارك مفاتيح الإدارة أو حسابات Firebase ذات الصلاحيات العالية داخل ملفات عامة.
